

## 5.4 我遇到的问题--睿博 JP_MP_R01串口血压计
P 05 引脚是 RX，通过将 UART-RX 引脚配置为输入上拉，在 STOP 低功耗模式下，通过发送协议指令使得 P 05 引脚拉低，从而唤醒休眠，配置串口设置等。（第一次接收到是唤醒，第二次接收到后回复协议指令）。
结果 P 05 当成 P 04 在算（P0123 4567），P05 是 bit 6 ，自己看成了 bit 5。导致怎么都唤醒不了。

学到的点：可以用 2 个数组存储接收中断的数据。ReceiveArray 和 ReceiveArray 1 ;ReceiveArray 1 通过 string. H 的操作 memcpy 复制接收存储后的 ReceiveArray 的数据。
再通过接收到协议指令的数据后，在执行下一步操作（下一个函数后），先进行 SendACK（ReceiveArray 1【3】, num）.
（ReceiveArray 1【3】）就是分类码

SendACK（ReceiveArray 1【3】, 1）：SendACK（com, num）;
com 是分类码、num 是要发的帧长。帧长内有效数据通过 SendTable【num】来确认个数即要发的数据（在发送前先给 SendTable 赋值）。


```
P0M  &=  0xDF;		//C sensor type, this pin is input.																
//		P0UR |= 	0x20;	//0x07;	  //00001110		// 1 input have pullhigh			
//		P0	 |=	0x20;
```



```
if(RI0)
		{
			if(UartType==3) UartCount=50;
			else UartCount=16;
			do{	
					if(RI0)
						{
							RI0 = 0;								//Clear Rx IRQ
							ReceiveArray[i]=S0RBUF;			
							ReceiveArray_index++;	
							if (ReceiveArray_index > sizeof(ReceiveArray) / sizeof(ReceiveArray[0]))
							{
									ReceiveArray_index = sizeof(ReceiveArray) / sizeof(ReceiveArray[0]);
							}			
							uart_time_out_cnt = 0;
							flag_is_uart_get_data = 1;

							i++;
							ReceiveCount=0;
						}		
					ReceiveCount++;
				}while((i<UartCount)&&(ReceiveCount<6000));
		}
	if(TI0)
		{
			TI0 = 0;                // Clear transmit flag
			goto UartOver;
    }
```









## 5.4 方翔云 flash 问题 

方翔云今天下午排查了一下午，直到晚上 23 点。Flash 存储一直不对（校验有问题），有关函数被重复调用了。而这个警告在最上面，被方翔云忽略了。理论上有一些前置条件，不会执行到后面那个重复调用的 flash 函数，但是确实有被执行。（放到定时器里的，与时间同步有关，会先判断帧数据是否符合要求）

总结：吸取教训，有重复调用的警告要注意看、去修改。之前培训说的是语音的函数被重复调用导致语音播报有问题。本质都是函数重复调用的影响。



## 5.6 方翔云的时间同步慢的问题
是因为 BP 00 B 主控，通过自动工装的模拟串口去接收数据，通过队列的形式取出数据到定时器中解析，结果解析的时间有点久（3-5 s）。最后发现通过修改定时器的配置可以改善。（修改定时器 2 的重装载值）



## 5.6 大昕 EPA-46 B 5


##### 写记忆代码
```
void write_full_mem_bank(e_user_t e_user)
{
    s_mem_t s_mem_temp = {0};
    s_time_t s_time_temp = {0};
    e_write_stat_typedef e_write_stat = WRITE_OTHER;
    uint8_t who_num_idex = 0;
    uint8_t who_buff_idex = 0;
    uint8_t temp = 0;
    uint8_t write_other_stat = 0;
    uint16_t write_count = 0;
    uint16_t i = 0;
	
    LED_ON;
    three_color_control(COLOR_GREEN);

    s_time_temp.day = WRITE_MEM_DAY_INIT;    /*  Day Value */
    s_time_temp.month = WRITE_MEM_MONTH_INIT;  /*  Month Value */
    s_time_temp.year = WRITE_MEM_YEAR_INIT;
    switch_sys_dia_hreshold(SELECT_THRESHOLD_GROUP);
		
    write_count = 1;
    clr_mem_bank(e_user);
    while (1)
    {
#if (IS_WRITE_CUFF_FLAG == 1)
				s_mem_temp.flag_cuff = 1; // 
				s_mem_temp.flag_cuff_isOk = 1;	//臂带正常
#endif
			
        s_time_temp.hour = write_count % 24;
        s_time_temp.min = write_count % 60;
        s_time_temp.day += 5;
        if (s_time_temp.day > 25)
        {
            s_time_temp.day = 1;
            s_time_temp.month++;
            if (s_time_temp.month > 12)
            {
                s_time_temp.month = 1;
                s_time_temp.year++;
            }
        }

        switch (e_write_stat)
        {
        case WRITE_OTHER:
            if (write_count >= g_s_func.mem_set - ((who_threshold_sum * 3) + 3))
            {
                e_write_stat = WRITE_7_TO_THRESHOLD_END;
                who_buff_idex = who_threshold_sum - 1;
                who_num_idex = 2;
            }
            // s_mem_temp.sys = SYS_HRESHOLD(0) + (write_count % (SYS_HRESHOLD(who_threshold_sum - 1) - SYS_HRESHOLD(0)));
            // s_mem_temp.dia = DIA_THRESHOLD(0) + (write_count % (DIA_THRESHOLD(who_threshold_sum - 1) - DIA_THRESHOLD(0)));
            if (write_other_stat == 0)
            {
                s_mem_temp.sys = SYS_HRESHOLD(0) + ((g_s_func.mem_set - ((who_threshold_sum * 3) + 3))- write_count);
                s_mem_temp.dia = DIA_THRESHOLD(0) + ((g_s_func.mem_set - ((who_threshold_sum * 3) + 3)) - write_count);
                if (s_mem_temp.sys <= s_mem_temp.dia)
                {
                    if (s_mem_temp.sys == 0xff || s_mem_temp.sys == 0 || s_mem_temp.dia == 0xff)
                    {
                        s_mem_temp.sys = SYS_MAX;	//254;
                        s_mem_temp.dia = DIA_MAX;	//199;
                    }
                    else
                    {
                        s_mem_temp.sys = s_mem_temp.dia + 1;
                    }
                    write_other_stat = 1;
                }
                else if (s_mem_temp.sys == 0 || s_mem_temp.dia == 0)
                {
                    s_mem_temp.sys++;
                    s_mem_temp.dia++;
                    write_other_stat = 0;
                }
								if ((s_mem_temp.sys >= SYS_MAX)||(s_mem_temp.dia >= DIA_MAX))
                {
									s_mem_temp.sys++;
									s_mem_temp.dia++;
									if (s_mem_temp.sys >= SYS_MAX)
									{
										s_mem_temp.sys = SYS_MAX;
										write_other_stat = 1;
									}
									if (s_mem_temp.dia >= DIA_MAX)
									{
										s_mem_temp.dia = DIA_MAX;
										write_other_stat = 1;
									}
									if ((s_mem_temp.sys == SYS_MAX)&&(s_mem_temp.dia == DIA_MAX))
									{
										s_mem_temp.sys = SYS_MAX;
                    s_mem_temp.dia = DIA_MAX;
										write_other_stat = 0;
									}
                }
            }
            else
            {
                s_mem_temp.sys = SYS_HRESHOLD(0) + write_count;
                s_mem_temp.dia = DIA_THRESHOLD(0) + write_count;
                if (s_mem_temp.sys <= s_mem_temp.dia)
                {
                    if (s_mem_temp.sys == 0xff || s_mem_temp.sys == 0 || s_mem_temp.dia == 0xff)
                    {
						s_mem_temp.sys = SYS_MAX;	//254;
                        s_mem_temp.dia = DIA_MAX;	//199;
                    }
                    else
                    {
                        s_mem_temp.sys = s_mem_temp.dia + 1;
                    }
                    write_other_stat = 0;
                }
                else if (s_mem_temp.sys == 0 || s_mem_temp.dia == 0)
                {
                    s_mem_temp.sys++;
                    s_mem_temp.dia++;
                    write_other_stat = 0;
                }
								if ((s_mem_temp.sys >= SYS_MAX)||(s_mem_temp.dia >= DIA_MAX))
                {
									s_mem_temp.sys++;
									s_mem_temp.dia++;
									if (s_mem_temp.sys >= SYS_MAX)
									{
										s_mem_temp.sys = SYS_MAX;
										write_other_stat = 0;
									}
									if (s_mem_temp.dia >= DIA_MAX)
									{
										s_mem_temp.dia = DIA_MAX;
										write_other_stat = 0;
									}
									if ((s_mem_temp.sys == SYS_MAX)&&(s_mem_temp.dia == DIA_MAX))
									{
										s_mem_temp.sys = SYS_MAX;
                    s_mem_temp.dia = DIA_MAX;
										write_other_stat = 0;
									}
                }
                
            }

            s_mem_temp.pul = MIN_PUL + (write_count % (MAX_PUL- MIN_PUL));

            break;

        case WRITE_7_TO_THRESHOLD_END:
            if (write_count >= g_s_func.mem_set - 6)
            {
                who_num_idex = 2;
                e_write_stat = WRITE_4_TO_6;
            }


            if (who_num_idex == 0) //阈值的最后一组
            {
                s_mem_temp.sys = SYS_HRESHOLD(who_buff_idex);
                s_mem_temp.dia = DIA_THRESHOLD(who_buff_idex);
                s_mem_temp.pul = 77;
            }
            else if (who_num_idex == 1)//阈值的中间的一组
            {
                s_mem_temp.sys = SYS_HRESHOLD(who_buff_idex) - 1;
                s_mem_temp.dia = DIA_THRESHOLD(who_buff_idex) - 1;
                s_mem_temp.pul = 66;
            }
            else if (who_num_idex == 2)//阈值开始的一组
            {
                temp = (SYS_HRESHOLD(who_buff_idex) - SYS_HRESHOLD(who_buff_idex - 1)) /2; //取相邻两组中位值
                s_mem_temp.sys = SYS_HRESHOLD(who_buff_idex) - temp;
                temp = (DIA_THRESHOLD(who_buff_idex) - DIA_THRESHOLD(who_buff_idex - 1)) /2;
                s_mem_temp.dia = DIA_THRESHOLD(who_buff_idex) - temp;
                s_mem_temp.pul = 55;
                who_buff_idex--;
            }
            break;
        case WRITE_4_TO_6:

            if (write_count >= g_s_func.mem_set - 3)
            {
                who_num_idex = 2;
                e_write_stat = WRITE_1_TO_3;
            }

            #if (IS_WRITE_ARRHYTHMIA_FLAG == 1)
            s_mem_temp.flag_pad = 1; // 
            #endif

            #if (IS_WRITE_CUFF_FLAG == 1)
            s_mem_temp.flag_cuff = 1; // 
						s_mem_temp.flag_cuff_isOk = 0;	//臂带过松
            #endif

            #if (IS_WRITE_MOVE_FLAG == 1)
            s_mem_temp.flag_move = 1; // 
            #endif

            if (who_num_idex == 0) //阈值的最后一组
            {
                s_mem_temp.sys = SYS_HRESHOLD(0);
                s_mem_temp.dia = DIA_THRESHOLD(0);
                s_mem_temp.pul = 77;
            }
            else if (who_num_idex == 1)//阈值的中间的一组
            {
                s_mem_temp.sys = SYS_HRESHOLD(0) - 1;
                s_mem_temp.dia = DIA_THRESHOLD(0) - 1;
                s_mem_temp.pul = 66;
            }
            else if (who_num_idex == 2)//阈值开始的一组
            {
                s_mem_temp.sys = SYS_HRESHOLD(0) - 20;
                s_mem_temp.dia = DIA_THRESHOLD(0) - 15;
                s_mem_temp.pul = 88;
            }
            break;

        case WRITE_1_TO_3:
            #if (IS_WRITE_AF_FLAG == 1)
                s_mem_temp.flag_af = 1; // 
            #endif
            if (write_count >= g_s_func.mem_set)
            {
                e_write_stat = WRITE_END;
            }


            if (who_num_idex == 0) //阈值的最后一组
            {
                s_mem_temp.sys = 122;
                s_mem_temp.dia = 82;
                s_mem_temp.pul = 62;
            }
            else if (who_num_idex == 1)//阈值的中间的一组
            {
                s_mem_temp.sys = 121;
                s_mem_temp.dia = 81;
                s_mem_temp.pul = 61;
            }
            else if (who_num_idex == 2)//阈值开始的一组
            {
                s_mem_temp.sys = 120;
                s_mem_temp.dia = 80;
                s_mem_temp.pul = 60;
            }
            break;

        case WRITE_END:
            goto WRITE_OVER;
            break;

        default:
            break;
        }
        memcpy((uint8_t *)&s_mem_temp.s_time,(uint8_t *)&s_time_temp,sizeof(s_time_t));

        aky_disp_clear_all();
        disp_user_icon(e_user);
        disp_to_sys(s_mem_temp.sys,1,1);  //
        disp_to_dia(s_mem_temp.dia,1,1);  //
        disp_to_pul(s_mem_temp.pul,1); //
//        if (g_s_func.e_time_sys != NO_TIME_DISP)
//        {
//            disp_mon_day(s_mem_temp.s_time,1);
//            disp_hour_min(s_mem_temp.s_time,1);
//        }
        disp_to_mem_set(write_count,1);
        show_who(s_mem_temp);
				
				
				if((s_mem_temp.flag_cuff == 1)&&(s_mem_temp.flag_cuff_isOk == 1))	//臂带正常
				{
					cuff_icon_on;
				}
				else if((s_mem_temp.flag_cuff == 1)&&(s_mem_temp.flag_cuff_isOk == 0))	//臂带过松
				{
					cuff_loose_icon_on;
				}
				else
				{
					cuff_icon_off;
				}
								
#if (FAST_WRITE_IGNORE_INDEX == 0)
        insert_one_mem(s_mem_temp,e_user);
#else
        g_e_flash_err = flash_save_mem_data(s_mem_temp, e_user, write_count,MAIN_ADDR);//记忆写入flash
        memset((uint8_t *)&s_mem_temp,0x00,sizeof(s_mem_t));

#endif

        write_count++;
        who_num_idex++;
        if (who_num_idex > 2)
        {
            who_num_idex = 0; 
        }
//        if (!KEY_POWERIO)
//        {
//            g_flag_stop_write_mem = 1;
//            goto WRITE_OVER;
//        }
    }
WRITE_OVER:

#if (FAST_WRITE_IGNORE_INDEX == 1)
    g_s_mem_index = get_default_mem_index_data();
    // g_s_mem_index.cur_mem_index = g_s_func.mem_set;
    g_s_mem_index.cur_mem_index = write_count - 1;
    g_e_flash_err = flash_save_mem_index_data(g_s_mem_index, e_user,MAIN_ADDR);//index下标自加后写回flash
#endif

    check_mem_bank(e_user,1);

    aky_disp_clear_all();
    AKY_ASM_NOP();
}
```

##### 测试蓝牙代码
```
#if 00
		
		
BT_POWER_ON;
	LED_ON;
	aky_disp_all();
aky_uart_init();
while(1)
{
	Rs232_SendByte(0x5A);
	delay(20);
	if(!KEY_POWER) {BT_POWER_OFF;break;}
	if(!KEY_MEM) {BT_POWER_ON;LED_ON;while(!KEY_MEM);}
	if(!KEY_SET) {BT_POWER_OFF;LED_OFF;while(!KEY_SET);}
}
		
#endif
```

蓝牙模块的主控
![[../../annex/34.入职第42周5.4-5.8（双休）_image_1.png]]



## 5.7 MOS 管
![[../../annex/34.入职第42周5.4-5.8（双休）_image_2.png]]


```
void SendAllMemory(void)	//20260506
{
		xdata uint8 k,l;	//k=需要发送的记忆数量;l=当前发送的笔数
		uint8 user_bak;
	
		s_mem_index_t s_mem_index_const;	//固定不变,记录发送前索引值
    s_mem_index_t s_mem_index_temp;
		
		user_bak = g_s_set.e_user;
		g_s_set.e_user = send_user;
	
//    g_s_mem_index = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);	
    s_mem_index_const = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);
    s_mem_index_temp = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);
	
		MemoryMode=1;				
		
		k=0;
		if (s_mem_index_const.cur_mem_index == 0 || g_e_flash_err == AKY_FLASH_ERR)	{SendACK(3);/*DisplayToPUL(0);*/	goto SendAllMemoryOver;}//记忆为空
		if(flag_ble_SendEtcMemoryFlag)//k=需要发送的记忆数量
		{
			if (s_mem_index_const.flag_mem_overwrite == 1)
			{
				s_mem_index_temp.cur_mem_index = s_mem_index_const.mem_set;	//满记忆直接索引到最后,先确认有多少笔记忆未上传
			}
			while(s_mem_index_temp.cur_mem_index>0)
			{
					g_s_mem = flash_get_mem_data(g_s_set.e_user, s_mem_index_temp.cur_mem_index, &g_e_flash_err, MAIN_ADDR);//readM24L16();			if(flag_ble_SendAllMemoryFlag || (g_s_mem.already_send==0x00))
					if(g_s_mem.already_send==0)	//统计未发送的笔数
					{
						k++;
					}
					s_mem_index_temp.cur_mem_index--;
					if(s_mem_index_temp.cur_mem_index == 0) break;
			}
			if(k==0) {SendACK(3);/*DisplayToPUL(0);*/	goto SendAllMemoryOver;}//全部发送过
		}
		else//发送全部记忆
		{
			k=s_mem_index_const.cur_mem_index;	//k=MemoryIndex;		
		}
		l=1;//当前发送的笔数
//    s_mem_index_const = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);
    s_mem_index_temp = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);	//索引回第一笔记忆位置

SendTest(s_mem_index_temp.cur_mem_index);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507test
SendTest(s_mem_index_temp.flag_mem_overwrite);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507test
SendTest(s_mem_index_const.cur_mem_index);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507testSendTest(6);	//20260507test MINTen
SendTest(s_mem_index_const.flag_mem_overwrite);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507testSendTest(6);	//20260507test MINTen
aky_tick_delay_ms(2000);	//20260507test
		
		while(s_mem_index_temp.cur_mem_index>0)
		{
SendTest(s_mem_index_temp.cur_mem_index);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507test
SendTest(g_s_mem.already_send);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507test

			g_s_mem = flash_get_mem_data(g_s_set.e_user, s_mem_index_temp.cur_mem_index, &g_e_flash_err, MAIN_ADDR);//readM24L16();			
			if(flag_ble_SendAllMemoryFlag || (g_s_mem.already_send==0))
			{
				//if(i>9) DisplayHalfByte(l/10,PulseTen);
				//DisplayHalfByte(l%10,PulseSingle);	
				delay(8);
				SendAll(l,k);	
				l++;
				flag_ble_SendOKFlag=0;
				off_time_tick_clr();
				while((get_cur_off_time_tick() < OFF_TIME_TICK_1S_COUNT * 3)&&!flag_ble_SendOKFlag&&KEY_POWER);
				if(!flag_ble_SendOKFlag)
				{
					//DisplayHalfByte(0xe,PulseTen);
					//DisplayHalfByte(0xb,PulseSingle);					
					goto SendAllMemoryOver;
				}	
				else
				{
					if(g_s_mem.already_send==0)//加上已发送标识
					{
						g_s_mem.already_send=1;
						flash_save_mem_data(g_s_mem, g_s_set.e_user, s_mem_index_temp.cur_mem_index,MAIN_ADDR);//记忆写入flash				
					}
				}
			}
			s_mem_index_temp.cur_mem_index--;	
			if (s_mem_index_temp.cur_mem_index == 0)
			{
					if (s_mem_index_const.flag_mem_overwrite == 1)
					{
							s_mem_index_temp.cur_mem_index = s_mem_index_const.mem_set;
SendTest(s_mem_index_temp.cur_mem_index);	//20260507test MINTen
aky_tick_delay_ms(200);	//20260507test
SendTest(7);	//20260507test MINTen
aky_tick_delay_ms(2000);	//20260507test						
					}
			}
			else if(s_mem_index_temp.cur_mem_index == s_mem_index_const.cur_mem_index)	//索引过一轮了
			{
				break;
			}
		}
SendAllMemoryOver:
SendTest(8);	//20260507test MINTen
	while(!KEY_POWER);
	delay(8);
	//DisplayHalfByte(0x10,PulseTen);
	//DisplayHalfByte(0x10,PulseSingle);				
	MemoryMode=0;
	flag_ble_SendAllMemoryFlag=0;
	flag_ble_SendEtcMemoryFlag=0;
		
	g_s_set.e_user = user_bak;
}
```


```
传199组记忆的时间
[2026-05-07 18:48:21.772]# RECV HEX>
[2026-05-07 18:50:44.526]# RECV HEX>
```

## 4 月份得分
![[../../annex/34.入职第42周5.4-5.8（双休）_image_3.png]]


## 5.8 大昕EPA-46 B 5设置模式函数


```
testMEM-Index：
void set_mode(void)
{
s_mem_index_t s_mem_index_const;	//20260507test
s_mem_index_t s_mem_index_temp;	//20260507test

	uint16_t fil_cnt = 0;
	uint16_t fil_cnt1 = 0;
	FULL_flag = 0;
	CHARGE_FULL_timer_flag = 0;
	show_timer_flag = 0;
	if(g_e_sys_stat == CHARGE_MODE)//充电界面功能
	{
		charge_mode_flag = 1;
	}
	
	if (check_low_bat_pro(LOW_BAT_VAL) == 1)
    {
        g_e_sys_stat = LOW_BAT_MODE;
        return;
    }
	off_time_tick_clr();
	while (KEY_SET == 0)
	{
		if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 3)
		{
			show_timer_flag = 1;
			VoiconIcon_enble_flag = 0;
			Alarm_enble_flag = 0;
			LED_ON;
			three_color_control(COLOR_GREEN);
		//    if (g_s_func.e_time_sys != NO_TIME_DISP)
		//    {
		//        disp_mon_day(g_s_time,1);
		//        disp_hour_min(g_s_time,1);
		//    }
			
			flag_is_switch_show_time_date = 0;
			//flag_is_refresh_display_now = 1;
//			disp_user_icon(g_s_set.e_user);
			//disp_show_mmhg_kpa();//显示单位
			goto time_disp_exit;
		}
		show_timer_flag = 0;
	}
	show_timer_flag = 0;
	VoiconIcon_enble_flag = 0;
	Alarm_enble_flag = 0;
	LED_ON;
    three_color_control(COLOR_GREEN);
//    if (g_s_func.e_time_sys != NO_TIME_DISP)
//    {
//        disp_mon_day(g_s_time,1);
//        disp_hour_min(g_s_time,1);
//    }
	
	flag_is_switch_show_time_date = 0;	//不显示时间
	flag_is_refresh_display_now = 1;
    disp_user_icon(g_s_set.e_user);
s_mem_index_const = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);	//20260507test
s_mem_index_temp = flash_get_mem_index_data(g_s_set.e_user, &g_e_flash_err, MAIN_ADDR);	//20260507test

//	disp_show_mmhg_kpa();//显示单位
//	off_time_tick_clr();
//	while (KEY_SET == 0)
//	{
//		if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 3)
//		{
//			goto time_disp_exit;
//		}
//	}
    off_time_tick_clr();
    while (1) // 时间显示
    {
disp_to_sys(s_mem_index_temp.cur_mem_index, 0, 0);//20260507test
disp_to_dia(s_mem_index_const.cur_mem_index, 0, 0);		//20260507test
disp_to_mem_set(s_mem_index_const.flag_mem_overwrite, 0);		//20260507test
disp_to_pul(s_mem_index_temp.flag_mem_overwrite, 0);	//20260507test
aky_disp_single_tube(g_s_mem.already_send/10%10, PulseTen);	//20260507test PulseSingle MINTen
aky_disp_single_tube(g_s_mem.already_send%10, PulseSingle);	//20260507test MINSingle

if(!KEY_MEM)	//20260507test
{
	s_mem_index_temp.cur_mem_index--;
	g_s_mem = flash_get_mem_data(g_s_set.e_user, s_mem_index_temp.cur_mem_index, &g_e_flash_err, MAIN_ADDR);
	while(!KEY_MEM);
}
		if(g_e_sys_stat == CHARGE_MODE)//充电界面功能
		{
			if(uart_rx_trig())
			{
				TUDetect();
				IsPowerOn = 0;
				g_e_sys_stat = STOP_MODE;
				return;
			}
			if(CHARGE != 0 && FULL == 0)//断电
			{
				delay(3);
				fil_cnt++;
				if(CHARGE != 0 && FULL == 0 && fil_cnt > 25)
				{
					IsPowerOn = 1;
					//g_e_sys_stat = STOP_MODE;
					//goto set_mode_exit;
					off_time_tick_clr();
					g_e_sys_stat = SET_MODE;
					return;
					
				}
			}
			else
			{
				fil_cnt = 0;
			}
			
			if(CHARGE_FULL_timer_flag == 1 && FULL_flag == 0)//充满
			{
				FULL_flag = 1;
				off_time_tick_clr();
				three_color_control(COLOR_GREEN);
			}
			
			
//			if (get_cur_off_time_tick() > OFF_TIME_TICK_6S_COUNT)
//			{
//				three_color_control(COLOR_ALL_OFF);
//			}
			if(FULL_flag == 1)
			{
//				if (get_cur_off_time_tick() > OFF_TIME_TICK_6S_COUNT)
//				{
//					three_color_control(COLOR_ALL_OFF);
//				}
				if (aky_off_time_trig(g_s_func.off_time - AUTOOFFIME_OFFSET))
				{
					FULL_flag = 0;
					IsPowerOn = 0;
					goto set_mode_exit;
				}
			}
			if (KEY_POWER == 0)
			{
				g_e_sys_stat = START_TEST_MODE;
				return;
			}
			if(g_s_flag_group.flag_rtc_it_trig && g_s_func.flag_alarm_enable &&KEY_POWER&&KEY_SET&&KEY_MEM)
			{
				for(AlarmN=0;AlarmN<2;AlarmN++)
				{
					if(AlarmOnOff[AlarmN]==1)
					{
						if(g_s_time.hour==AlarmHour[AlarmN] && g_s_time.min==AlarmMinute[AlarmN])
						{	
							Alarmtemp = AlarmN;
							AlarmFlag=1;
						}
					}
				}
			}
			
			if(AlarmFlag&&KEY_MEM&&KEY_POWER&&KEY_SET)//闹钟
			{
				LED_ON;
				three_color_control(COLOR_GREEN);
				g_s_flag_group.flag_rtc_it_trig = 0;
				flag_is_switch_show_time_date = 0;
				off_time_tick_clr();
				aky_disp_clear_all();
				disp_hour_min(g_s_time,1);
				AlarmN = Alarmtemp;
				showalarm(ALARM_VOICE);	
				while(KEY_MEM&&KEY_POWER&&KEY_SET)
				{
                    if (g_s_func.e_spk_type == CHINESE)
                    {
					    spk_single_cmd(BEEP0);
                    }
                    else if (g_s_func.e_spk_type == CHINESE_7817)
                    {
                        new_cn_ic_voice_send_cmd_server(VOICE_7817CN_CMD_DIDIDI);
                        aky_delay_tick_clr();
                        while (aky_get_cur_tick() < OFF_TIME_TICK_100MS_COUNT * 5)
                        {
                            if (KEY_POWER == 0 || KEY_SET == 0 || KEY_MEM == 0)
					        {
					        	IsPowerOn = 0;
					        	goto set_mode_exit;
					        }
                        }               
                    }
										else if (g_s_func.e_spk_type == ENGLISH_US)
										{
											spk_single_cmd(BEEP0_75US);
										}
					//delay(10);

					if (aky_off_time_trig(g_s_func.off_time))
					{
						if(AlarmN==1){Alarmtemp=0;}
						else{Alarmtemp=1;}
						if(AlarmOnOff[Alarmtemp]==1)
						{
							if(AlarmHour[Alarmtemp]==AlarmHour[AlarmN])
							{
								if(AlarmMinute[Alarmtemp]==(AlarmMinute[AlarmN]+1))
								{
									off_time_tick_clr();
								}
							}
						}
						break;
					}

//					if (get_cur_off_time_tick() > OFF_TIME_TICK_6S_COUNT)
//					{
//						three_color_control(COLOR_ALL_OFF);
//					}
					if (KEY_POWER == 0 || KEY_SET == 0 || KEY_MEM == 0)
					{
						IsPowerOn = 0;
						goto set_mode_exit;
					}
				}
				IsPowerOn = 0;
				goto set_mode_exit;
			
			}
			if (KEY_SET == 0)
			{
				off_time_tick_clr();
				LED_ON;
				three_color_control(COLOR_GREEN);
				while (KEY_SET == 0)
				{
					while(KEY_SET == 0 && KEY_MEM == 0)
					{
//						while(KEY_POWER == 0 && KEY_MEM == 0 && KEY_SET == 0)//一键还原
//						{
//							if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
//							{
//								g_e_sys_stat = SET_MODE;
//								initialize_flag = 1;
//								IsPowerOn = 1;
//								return;
//							}
//						}
						if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
						{
							flag_is_switch_show_time_date = 0;
							g_e_sys_stat = LINEAR_MODE;
							IsPowerOn = 1;
							return;
						}
					}
					
					if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 3)
					{
						goto time_disp_exit;
					}
				}
				g_s_set.e_user++;
				if (g_s_func.e_user_num == TWO_USER)
				{
					if (g_s_set.e_user > USER_2)
					{
						g_s_set.e_user = USER_1;
					}
				}
				else if (g_s_func.e_user_num == THREE_USER)
				{
					if (g_s_set.e_user > USER_3)
					{
						g_s_set.e_user = USER_1;
					}
				}
				disp_user_icon(g_s_set.e_user);
			}
			
//			if (KEY_MEM == 0)
//			{
//				off_time_tick_clr();
//				LED_ON;
//				three_color_control(COLOR_GREEN);
//				while (KEY_MEM == 0)
//				{
//					while(KEY_SET == 0 && KEY_MEM == 0)
//					{
////						while(KEY_POWER == 0 && KEY_MEM == 0 && KEY_SET == 0)//一键还原
////						{
////							if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
////							{
////								g_e_sys_stat = SET_MODE;
////								initialize_flag = 1;
////								IsPowerOn = 1;
////								return;
////							}
////						}
//						if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
//						{
//							flag_is_switch_show_time_date = 0;
//							g_e_sys_stat = LINEAR_MODE;
//							IsPowerOn = 1;
//							return;
//						}
//					}
//				}
//				g_e_sys_stat = MEM_MODE;
//				return;
//			}

		}
		else
		{
			if(g_s_flag_group.flag_rtc_it_trig && g_s_func.flag_alarm_enable &&KEY_POWER&&KEY_SET&&KEY_MEM)
			{
				for(AlarmN=0;AlarmN<2;AlarmN++)
				{
					if(AlarmOnOff[AlarmN]==1)
					{
						if(g_s_time.hour==AlarmHour[AlarmN] && g_s_time.min==AlarmMinute[AlarmN])
						{	
							Alarmtemp = AlarmN;
							AlarmFlag=1;
						}
					}
				}
			}
			
			if(AlarmFlag&&KEY_MEM&&KEY_POWER&&KEY_SET)//闹钟
			{
				REALARM:
				LED_ON;
				three_color_control(COLOR_GREEN);
				g_s_flag_group.flag_rtc_it_trig = 0;
				flag_is_switch_show_time_date = 0;
				off_time_tick_clr();
				aky_disp_clear_all();
				disp_hour_min(g_s_time,1);
				AlarmN = Alarmtemp;
				showalarm(ALARM_VOICE);	
				while(KEY_MEM&&KEY_POWER&&KEY_SET)
				{
					if (g_s_func.e_spk_type == CHINESE)
                    {
					    spk_single_cmd(BEEP0);
                    }
                    else if (g_s_func.e_spk_type == CHINESE_7817)
                    {
                        new_cn_ic_voice_send_cmd_server(VOICE_7817CN_CMD_DIDIDI);
                        aky_delay_tick_clr();
                        while (aky_get_cur_tick() < OFF_TIME_TICK_100MS_COUNT * 5)
                        {
                            if (KEY_POWER == 0 || KEY_SET == 0 || KEY_MEM == 0)
					        {
					        	IsPowerOn = 0;
					        	goto set_mode_exit;
					        }
                        }               
                    }
										else if (g_s_func.e_spk_type == ENGLISH_US)
										{
											 spk_single_cmd(BEEP0_75US);
										}
					//delay(10);

					if (aky_off_time_trig(g_s_func.off_time))
					{
						if(AlarmN==1){Alarmtemp=0;}
						else{Alarmtemp=1;}
						if((AlarmOnOff[Alarmtemp]==1) && 
							(AlarmHour[Alarmtemp]==AlarmHour[AlarmN]) && 
							(AlarmMinute[Alarmtemp]==(AlarmMinute[AlarmN]+1)))
						{
							off_time_tick_clr();
							goto REALARM;
						}
						else
						{
							IsPowerOn = 0;
							goto set_mode_exit;
						}
					}
					if (check_low_bat_trig(LOW_BAT_VAL) == 1)//
					{
						IsPowerOn = 0;
						mode_lowbat_flag = 1;
						g_e_sys_stat = LOW_BAT_MODE;
						return;
					}

//					if (get_cur_off_time_tick() > OFF_TIME_TICK_6S_COUNT)
//					{
//						three_color_control(COLOR_ALL_OFF);
//					}
					if (KEY_POWER == 0 || KEY_SET == 0 || KEY_MEM == 0)
					{
						IsPowerOn = 0;
						goto set_mode_exit;
					}
				}

			}
			
			if (all_while_loop_exit() == 1)
			{
				goto set_mode_exit;
			}

			key_exit_delay_ms(30);

			if (aky_off_time_trig(g_s_func.off_time))
			{
				IsPowerOn = 0;
				goto set_mode_exit;
			}

//			if (get_cur_off_time_tick() > OFF_TIME_TICK_6S_COUNT)
//			{
//				three_color_control(COLOR_ALL_OFF);
//			}

			if (KEY_POWER == 0)
			{
				off_time_tick_clr();
				while (KEY_POWER == 0)
				{
//					while(KEY_POWER == 0 && KEY_MEM == 0 && KEY_SET == 0)//一键还原
//					{
//						if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
//						{
//							g_e_sys_stat = SET_MODE;
//							initialize_flag = 1;
//							IsPowerOn = 1;
//							return;
//						}
//					}
				}

				IsPowerOn = 0;
				goto set_mode_exit;
			}
			
			if (KEY_SET == 0)
			{
				off_time_tick_clr();
				LED_ON;
				three_color_control(COLOR_GREEN);
				while (KEY_SET == 0)
				{
					while(KEY_SET == 0 && KEY_MEM == 0)
					{
//						while(KEY_POWER == 0 && KEY_MEM == 0 && KEY_SET == 0)//一键还原
//						{
//							if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
//							{
//								g_e_sys_stat = SET_MODE;
//								initialize_flag = 1;
//								IsPowerOn = 1;
//								return;
//							}
//						}
						if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
						{
							flag_is_switch_show_time_date = 0;
							g_e_sys_stat = LINEAR_MODE;
							IsPowerOn = 1;
							return;
						}
					}
					
//					if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 3)
//					{
//						goto time_disp_exit;
//					}
				}
				g_s_set.e_user++;
				if (g_s_func.e_user_num == TWO_USER)
				{
					if (g_s_set.e_user > USER_2)
					{
						g_s_set.e_user = USER_1;
					}
				}
				if (g_s_func.e_user_num == THREE_USER)
				{
					if (g_s_set.e_user > USER_3)
					{
						g_s_set.e_user = USER_1;
					}
				}
				disp_user_icon(g_s_set.e_user);
			}
			
//			if (KEY_MEM == 0)
//			{
//				off_time_tick_clr();
//				LED_ON;
//				three_color_control(COLOR_GREEN);
//				while (KEY_MEM == 0)
//				{
//					while(KEY_SET == 0 && KEY_MEM == 0)
//					{
////						while(KEY_POWER == 0 && KEY_MEM == 0 && KEY_SET == 0)//一键还原
////						{
////							if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
////							{
////								g_e_sys_stat = SET_MODE;
////								initialize_flag = 1;
////								IsPowerOn = 1;
////								return;
////							}
////						}
//						if (get_cur_off_time_tick() > OFF_TIME_TICK_1S_COUNT * 1)
//						{
//							flag_is_switch_show_time_date = 0;
//							g_e_sys_stat = LINEAR_MODE;
//							IsPowerOn = 1;
//							return;
//						}
//					}
//				}
//				g_e_sys_stat = MEM_MODE;
//				return;
//			}
			if (check_low_bat_trig(LOW_BAT_VAL) == 1)//
			{
				g_e_sys_stat = LOW_BAT_MODE;
				mode_lowbat_flag = 1;
				return;
			}
		}
	}
time_disp_exit:
	//setmode_flag = 1;
	Nocher_bat_flag = 1;
    if (check_low_bat_pro(LOW_BAT_VAL) == 1)
    {
        g_e_sys_stat = LOW_BAT_MODE;
        return;
    }
	flag_is_switch_show_time_date = 0;
    aky_disp_clear_all();
    LED_ON;
    three_color_control(COLOR_GREEN);
	VoiconIcon_enble_flag = 0;
	Alarm_enble_flag = 0;
		
		unit_set();
    #if (AKY_LED_ENABLE == 0)
        time_set();
    #endif
    if (g_s_func.e_spk_type != NO_VOICE)
    {
        spk_set();
    //unit_set();
	#if (AKY_LED_ENABLE == 0)
			if(g_s_func.flag_alarm_enable == 1)
			{
				AlarmSet();
			}
    #endif
    }

set_mode_exit:
	auto_test_deinit();
	Nocher_bat_flag = 0;
	//setmode_flag = 0;
    aky_disp_clear_all();
    store_set_to_flash(g_s_set);
    IsPowerOn = 0;
	
    if(mode_lowbat_flag == 0)
	{
		g_e_sys_stat = STOP_MODE;
	}
	else
	{
		VoiconIcon_enble_flag = 1;
		mode_lowbat_flag = 0;
	}
}
```

### APP 数据
![[../../annex/34.入职第42周5.4-5.8（双休）_image_4.jpg]]

![[../../annex/34.入职第42周5.4-5.8（双休）_image_5.jpg]]


## ZSHX-D 01 锂电三色背光
参考代码改为2025.7.2，把显示移了过去
![[../../annex/34.入职第42周5.4-5.8（双休）_image_6.png]]


















