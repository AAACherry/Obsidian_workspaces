
## Self
1、进万次寿命，压力值读取有问题。是 ADC 采集电量打开了，导致 ADC 读不准压力值。张清淞的。


2、BD1国高的。充电管理 IC 不是5056（原理图上是5056），实际为2312，所以电平状态对不上。另外他的通过外部中断 EXTI0、设置双边沿中断用来唤醒。唤醒后 DISABLE 没作用、还是会唤醒的问题是，他自己写了个函数，导致原本的函数没用了，被优化掉了。马林的 BD1。

3、KE 4002，快测检低电的目的是确认分压电阻有没有贴错。所以发现比较器 LBTM&0 x 02 检低电发现 3.0 V 才报低电的情况，不应该改用 ADC 去检低电，这样目的就搞错了。
其他界面（正常使用时）低电 3.3 V 能检到并报低电是因为 CheckBatteryADC 函数 ADC 检测触发的，而不是 CheckBatteryCOM 比较器触发的。因为 CheckBatteryADC 中不只有电量显示的检测与判断，还有低电报警，当时没仔细看，以为 CheckBatteryADC 中只有电量显示、而电量检测只在 CheckBatteryCOM 中。
因为测量过程中检低电是用比较器检低电，所以电压被拉低到了 3.0 后会报低电，而进入之前是用 ADC+比较器去检低电，3.3 V 时 ADC 低电被触发了、所以进不了测量。而 3.4 V 进入测量时，ADC 低电没有触发、正常测量，直到泵拉电电压到了 3.0 V 时比较器触发低电报警。


4、参考 AXD-840E 软件，发现只初始化时，语音会一直循环播报，背光会一直乱闪烁。原因是相关引脚被配置为了 LCD 的 SEG 引脚，与实际项目不一致，所以造成这种现象。马林的。

5、BB-Q 1 和 BB-Q 5 预适应训练仪，去掉电容后，M 2 按键具备和语音开关键同样的功能: 在 M 4 自定义按键进入自定义后，卡在 do while 中，且按下 M 2 按键居然也会语音开关。实际是程序运营太快。
在 do while 中只执行/判断了几句代码，在 voice_switch 函数中加个 delay 即可。

6、任何一条收发指令都要发3次，防止丢包

https://blog.csdn.net/qq_36089229/article/details/107135255

https://www.w3ccoo.com/excel/excel_addition.html

https://www.genuway.com/3514.html
RTC 晶振 32.768 KHz 频率精度 PPM 值及时间偏差计算方法


Protel PCB Document


6、马林的基于 840 E 进行修改。但是阀和泵互换了，结果泵还是全速工作，挂逻分没有波形。
结果是 VALVE_ON 和 PUMP_ON 没互换。

https://mp.weixin.qq.com/s/AiULk4N6EuvuVIMYUEyaDg
在嵌入式设备的通信中，为什么总用 0 x 55,0 xaa 作为帧头呢？

USB 接口测试：信号质量，浪涌电流，电压跌落等


LDO→DAC→OP-
VDDR


7、马林的真实幻想 S 04，参考 AXD-840 E 去改。遇到问题: 无自动工装。移植别的项目的自动工装代码后，有个自动工装唤醒问题。
原来 840 E 的通过二极管接在了 KEY_POWER 引脚上，而 S 04 是通过二极管接在了 KEY_USER 上。所以需要改一下配置才能唤醒、使用。


![[../../annex/25.2025.12.28-2026.3.18 Self_image_1.png]]

8、BSX-B 501 显示问题排查思路：
1、SEG 引脚初始化是否有反复输出低、输入上拉的操作。
2、或者是否统一配置 SEG 引脚。
3、看 LCD 的配置，看看 LCD 时钟源是什么？LCD 的时钟分频是多少？LCD 的 VLCD 驱动电压选择的是多少？
4、示波器抓取 SEG 引脚的波形
5、disp_clear_all（Darkallicon）后需要延时 delay 一会再去显示，不然可能会出现显示冲突（渐灭留痕的问题）
6、如果是某几个 SEG 引脚出现显示问题，则可以排查看看是否有外围电路的影响（查看该 Pin 是否接在了多余其他电路上，如 LCD 和 LED 共板，但是 LED 电路部分没去干净）
7、如果怀疑是 IC 问题，可以尝试换块板子、或换个主控 IC。


8、BSX-A 70 G 语音播报（泡水音）问题：
1、接了功放，实际 aky 的语音不需要接功放。去掉后直接接在喇叭端子上即可。
2、语音不播报问题，但是语音 IC 检测（发停止指令）能检测到：二极管没焊。
3、4 G 的 SIM 和 4 G 模块要共地。



@晶华微-刘健婷你好，陈工今天请假了，有些 F 479 相关的问题，要请教下你们。
发现一处你们库的问题 UART_DeInit，已经跟跟陈工确认修改，还是 UART 0 中断不能正常工作。

现在遇到的问题：UART 1 可以正常收发，而 UART 0 可以发，但不能进中断收，

9、A 70 G，TX 和 RX 的三极管还是要的，不然电平不匹配，导致数据通讯失败。
其次需要把原来的 PWR 割断，后再飞线接到

用压力值测试 B 501 充气无法达到指定压力报错的条件

04 芯片，翻记忆会跳，所以加了电容。
通信引脚不能加电容，否则通信不了 (debug 引脚等)


IOS 端数据完整打包。
安卓端最多发送 20 个字节大小的内容，大于 20 个字节大小的内容无显示。可通过修改 MTU ，修改后可以看到传输的数据


现象: 进去 CA 校准后，若校压失败 (直接短按开关键退出也可)。进去测量模式，在校零后直接报错 (Err 5 或 Err 7+Hi)。且该现象只发生在插入 USB (有无锂电池都会)、偶数次校压失败的情况下。
未插入 USB 时正常，区别: 插入 USB 后不休眠及唤醒。直接退出 STOP 进入时间界面。

临时解决方式: 在 STARTTEST 加入读取校压值。读取校准值后就不会发生这种现象。


另外: 00 B 支持带电烧录: 长按开关键+短按设置键 3 次进入 isp 复位。临时改为5次。


模块漏电回来。
MCU 漏电过去才会电流大。
先确保 TX，RX 拉低。供电脚拉低。使能引脚也拉低。

BSX-B 501，进校压后会改变校压值，校压失败后进测量模式就会出现零点校准完毕后立即报错的现象。在测量前加上读回 Flash 中的校压值即可解决。也可在校压失败退出时加。

马林犯了跟我之前一样的问题，发送未选 HEX，发成了默认的 ASCII。
为了避免这种情况，其实可以加上数值限定: 如果不是枕头数据，回一个提示，

A 70 G:
RX, TX 反了
POWER 供电开关引脚需要割断，接回 P77
PWR 唤醒引脚需要飞回原来的第四脚

可以去掉三极管，直接用串口助手电脑收发，查看模块是否正常工作。

Disp_clear_all 中不灭电池的＝少写了一个，导致开机不了，无法启动芯片 (屏不显)

IMEI 号→生成二维码。保身欣小程序→扫码添加设备。等数据收发。

换了新机器，IMEI 号生成二维码没改，导致一直没数据更新到小程序。

关机电流大 (2 mA)，为 debug 未关闭宏定义。实际未进休眠。关闭后关机电流约 30 uA。

奇数次开，偶数次关。进休眠也一直关不断 4 G 模块 (指示灯在亮)。实际是 4 G 模块的供电 4.2 V 外接在了电源，忘记去掉了。

![[../../annex/25.2025.12.28-2026.3.18 Self_image_2.png]]


BP 04 C 才是硬件选配电阻控制低电压检测的。
其他是软件去调 (低电)的。

可以烧录，但是写记忆有问题 (判断主控是否为对应工程代码)。(地址有问题，写记忆会乱)。

自动工装 (模拟串口)，104 us，刚好错过了

全局变量没有置零。

重复调用队列。

AXD-840 E 电量格数清屏，1 格。
定时器，要加判断干锂电。
BP 00 B 第一次需要用烧录器烧录一次才能用单线调试 (把 P 77 调试功能打开)。其次需要把工程中的宏定义 onewrite 打开。



S603-LED，在自动工装收发串口数据时需要把中断关闭。


3.17 方少的局部变量 res 没有初始化，导致 emc 时第一次进测量正常校零，结果第二次校零直接报错。


好升平晶振不起振，接错位置了。（晶振接到了电容后面，地那边）
好升平普通阀漏气 (加压加不上去，用密封的气管堵住漏速管/普通漏速阀/更换五通管的方式先排查是不是有漏气、哪里漏气)。普通阀高低电平导通/关闭，故而写个测试代码。发现置 1 置 0 都是会漏气，所以再排查硬件问题。对比 KE 2003 的同款马达驱动 IC 驱动后泵阀两端的电压，发现好升平的不正常。更换马达驱动 IC 后就好了。

##### KE 4002 进不了自动工装，去晶振修改了主频导致的。

KE 4002。中断里改变了 TC 0 M，导致内囊泵初始化时会动一下。
好升平关机电流大: LCD 没关 (lcd_deinit ()函数，导致 6.75 mA 的关机电流，正常应为 20 uA)

##### 保身欣 S 603，V 106 中文语音。
在设置默认中，调用 SoundNoWait 指令后 while (！BUSY)语音播放过程中有按键判断、闪烁显示，而 KEY_MEMORYUP 键没有做退出判定，在按下按键后 BUSY 引脚的电平状态没有立马切换，导致多次发送 0 xFE 停止语音播报指令，且多次发送时波形是乱的 (发了十几次 0 xFE、中间没有延时)，误判为语音音量调节指令。
所以会出现打断语音后，下一个设置的语音播报音量会变。而变大后又变回去大概率是因为设置到了单位前是设置语音，会进行一次保存更新音量。

解决/排查思路: 尽可能模拟该情况下的语音播报。写一个 while (1)，在循环中发送“设置时间制式”的语音，且在播报后连续发送 100 次 0 xFE 停止播报的指令，中间不要有延时 delay。因为出问题的情况下就没有 delay。如果加了 delay，发送出去的波形是正常的，会复现不了这种音量突然变大的现象。














































































































































































































































































































































































































































































































































































































































































































































































































































































































































































