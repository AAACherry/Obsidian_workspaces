
## AXD-303A 雾化器
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_1.png]]
AXD-303A 雾化器，蜂鸣器，4K 频率、80%占空比

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_2.jpg]]


```
if((Buz_WorkFlag == 1) || (Buz_WorkFlag == 2)) //Buz工作150ms
	{
		t1_tick_buzz_ms++;
		
		if((Buz_WorkFlag == 2) && (t1_tick_buzz_ms >= 350))
		{
			t1_tick_buzz_ms = 0;
			Buz_WorkFlag = 0;
		}
		else if((Buz_WorkFlag == 1) && (t1_tick_buzz_ms >= 150))
		{
			t1_tick_buzz_ms = 0;
			Buz_WorkFlag = 0;
		}
	}
```

##### 蜂鸣器不停止的原因：数据溢出
```
蜂鸣器不停止的原因：数据溢出，t1_tick_buzz_ms 定义时为 uint8；改为 uint32即可。
```


##### A04 烧录代码
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_3.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_4.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_5.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_6.png]]


AXD-840E
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_7.png]]
3.6 V→3.54 V 亮屏→3.34 V，3.16 V。
3.44 V
3.45 V 亮屏+语音播报
阀: 高电平关闭
马达: 不工作时 2 个引脚都是高电平，工作时一个引脚被拉低为低电平。



## FY_B 01--平头哥 CKLink
黄线 SWCLK--TMS，蓝线 SWDIO--TMS，黑线 GND-GND


![[../../annex/20.入职第二十周11.24-11.29（单休）_image_8.png]]

```
if(g_s_func.e_uart_type == BLE && BTFlag)
{
		res = 1;
		break;
}
```

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_9.jpg]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_10.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_11.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_12.jpg]]


## KE-2003 三合一加蓝牙
KE-2003 血压、血糖、尿酸三合一
GLU_TEST_MODE（血糖测试模式）
ECC_MODE（错误校正模式）
UA_TEST_MODE（尿酸测试模式）

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_13.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_14.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_15.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_16.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_17.jpg]]


```
key_select_mode = 0;
	key_time_tick_clr();
	//wait_all_key_release();
	if(g_s_func.e_uart_type == BLE)
	{
		while(!KEY_POWER)
		{
			if(get_key_time_tick() < OFF_TIME_TICK_1S_COUNT*3)	//5s
			{
				g_e_sys_stat = START_TEST_MODE;
				key_select_mode = 0;
			}
			else if(get_key_time_tick() >= OFF_TIME_TICK_1S_COUNT*3)	//5s
			{
				key_select_mode = 1;
			}
		}
	
		if(key_select_mode == 1)
		{
			BT_POWER_ON;
			BleParamInit();
		}
		
		IsPowerOn = 1;
		aky_disp_clear_all();
		off_time_tick_clr();
		
		do 
		{
//			Show_ble(); //放到定时器中断中去显示
			
//			if (T500ms == 0) //test
//			{
//				SendEmpty();
//			}
			BleACK();	//20251126
			
			if (CODE5_IO == 1)
			{
					Past_CODE5 = 1;
			}
				
			if (CODE5 == 0 && Past_CODE5 == 1)// || CODE7 == 0||CODE9 == 0)
			{
					Mode_Detect();
					// g_e_sys_stat = GLU_TEST_MODE;
			}
			if(g_e_sys_stat == ECC_MODE) return;
			if(g_e_sys_stat == UA_TEST_MODE) return;
			if(g_e_sys_stat == GLU_TEST_MODE) return;
			
			if (all_while_loop_exit() == 1)
			{
					IsPowerOn = 0;
					key_select_mode = 0;
					goto start_test_exit;
			}
		
			key_exit_delay_ms(30);
			
			if (aky_off_time_trig(g_s_func.off_time))
			{
					IsPowerOn = 0;
				  key_select_mode = 0;
					goto start_test_exit;
			}
					
		}while(KEY_POWER&&KEY_MEM&&KEY_SET&&(key_select_mode == 1)&& !BTFlag && !SendMemoryFlag && IsPowerOn);
		
		if (SendMemoryFlag)
		{
				delay(50); // about 0.5s
				SendMemoryBLE();
				IsPowerOn = 0;
		}
		if (IsPowerOn == 0)
		{
				g_e_sys_stat = STOP_MODE;
				return;
		}
	}
	key_select_mode = 0;
	aky_disp_clear_all();
```
start_test_mode 模式下蓝牙连接界面

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_18.png]]


```
/*血糖GLU/尿酸UA数据传输通信协议*/
//帧头
void SendHead(void)
{
	Rs232_SendByte(0xA5);
	Rs232_SendByte(0xA5);
	Rs232_SendByte(framesize); 		//帧长
	CheckSum = 0;
}

//帧尾
void SendEnd(void)
{
	Rs232_SendByte(CheckSum);			//校验和
	Rs232_SendByte(0x5A);
	Rs232_SendByte(0x5A);
}

uint8_t retry_count = 0;
uint8_t user;
//发送血糖/尿酸开始测量信息
void Send_UA_GLU_starttest(void)
{
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;

	SendStarttestFlag = 0;
RETRY:			
	framesize = 4;								//帧长
	SendHead();
	
	Rs232_SendByte(0xB1);					//数据类型--0xB1--开始测量
	Rs232_SendByte(user);
	Rs232_SendByte(papersort);		//papersort 为插入的试纸类型 ， 1，血糖插入，2 尿酸插入 3，同时插入
	if(papersort == 1)
	{
		Rs232_SendByte(GluCode);		//GluCode为 血糖试纸code
	}
	else if(papersort == 2)
	{
		Rs232_SendByte(UACode);			//UACode为尿酸试纸code
	}
	else
	{
		Rs232_SendByte(0x00);				
	}
	
	SendEnd();
	
	off_time_tick_clr();
	do
	{
		if(SendStarttestFlag == 1) break;
	}while((get_cur_off_time_tick() <= OFF_TIME_TICK_100MS_COUNT) && KEY_POWER);
	
	if((retry_count < 3) && (SendStarttestFlag != 1) && KEY_POWER)
	{
		retry_count++;
		goto RETRY;
	}
}

//发送试纸类型
/*
void SendBloodsort(void)
{
	uint8_t user;
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;
		
	framesize = 3;								//帧长
	SendHead();
	
	Rs232_SendByte(0xB2);
	Rs232_SendByte(user);
	Rs232_SendByte(Bloodsort);		//Bloodsort 为滴血的试纸类型 1 血糖滴血， 2尿酸滴血，3 同时滴血
	
	SendEnd();
}*/

//发送血糖检测结果数据
void SendGLUResultdata(void)
{
	//uint8_t retry_count = 0;
	uint8_t user;
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;

	SendGLUOKFlag = 0;
RETRY:		
	framesize = 5;									//帧长,要相应修改
	SendHead();	
			
	Rs232_SendByte(0xB2);						//数据类型--0xB2--发送血糖结果
	Rs232_SendByte(user);
	
	if (GluValue_mol < MINGLU)			//血糖过低发送0x00
	{
		Rs232_SendByte(0x00);
		Rs232_SendByte(0x00);
	}
	else if (GluValue_mol > MAXGLU)	//血糖过高发送0xff
	{
		Rs232_SendByte(0xFF);
		Rs232_SendByte(0xFF);
	}
	else 
	{
		Rs232_SendByte(GluValue_mol*10/256);	//血糖高八位，血糖数据 放大了10倍
		Rs232_SendByte(GluValue_mol*10%256);	//血糖低八位
	}
	
	Rs232_SendByte(0x00);										//单位:0x00--mmol/L;0x01--mg/dL
	
	if(g_s_func.flag_time_set_enable == 1)	//flag_time_set_enable=0;不传输、不计入帧长
	{
		Rs232_SendByte(g_s_time.year);				//DataPrepareMem
		Rs232_SendByte(g_s_time.month);			
		Rs232_SendByte(g_s_time.day);
		Rs232_SendByte(g_s_time.hour);			
		Rs232_SendByte(g_s_time.min);
		//Rs232_SendByte(second);
	}
	
	SendEnd();
	
	off_time_tick_clr();
	do
	{
		if(SendGLUOKFlag == 1) break;
	}while((get_cur_off_time_tick() <= OFF_TIME_TICK_100MS_COUNT) && KEY_POWER);
	
	if((retry_count < 3) && (SendGLUOKFlag != 1) && KEY_POWER)
	{
		retry_count++;
		goto RETRY;
	}
}

//发送尿酸检测结果数据
void SendUAResultdata(void)
{
	//uint8_t retry_count = 0;
	uint8_t user;
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;

	SendUAOKFlag = 0;
RETRY:		
	framesize = 5;											//帧长,要相应修改
	SendHead();
	
	Rs232_SendByte(0xB3);								//数据类型--0xB3--发送尿酸结果
	Rs232_SendByte(user);			
	
	if (UAValue_mol < MINUA)						//尿酸过低,发送0x00
	{
		Rs232_SendByte(0x00);
		Rs232_SendByte(0x00);	
	}
	else if (UAValue_mol > MAXUA)				//尿酸过高,发送0xff
	{
		Rs232_SendByte(0xFF);			
		Rs232_SendByte(0xFF);	
	}
	else
	{
		Rs232_SendByte(UAValue_mol/256);	//尿酸高八位
		Rs232_SendByte(UAValue_mol%256);	//尿酸低八位
	}
	
	Rs232_SendByte(0x00);								//单位:0x00--umol/L;0x01--mg/dL
	
	if(g_s_func.flag_time_set_enable == 1)	//flag_time_set_enable=0;不传输、不计入帧长
	{
		Rs232_SendByte(g_s_time.year);
		Rs232_SendByte(g_s_time.month);			
		Rs232_SendByte(g_s_time.day);
		Rs232_SendByte(g_s_time.hour);			
		Rs232_SendByte(g_s_time.min);
		//Rs232_SendByte(second);
	}
	
	SendEnd();
	
	off_time_tick_clr();
	do
	{
		if(SendUAOKFlag == 1) break;
	}while((get_cur_off_time_tick() <= OFF_TIME_TICK_100MS_COUNT) && KEY_POWER);
	
	if((retry_count < 3) && (SendUAOKFlag != 1) && KEY_POWER)
	{
		retry_count++;
		goto RETRY;
	}
}

//发送血糖报错信息
void SendGLUError(uint8_t errorcode)
{
	//uint8_t retry_count = 0;
	uint8_t user;
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;

	SendErrorOKFlag = 0;
RETRY:		
	framesize = 3;										//帧长
	SendHead();
	
	Rs232_SendByte(0xB4);							//数据类型--0xB4--发送血糖报错
	Rs232_SendByte(user);
	Rs232_SendByte(errorcode);
	
	SendEnd();
	
	off_time_tick_clr();
	do
	{
		if(SendErrorOKFlag == 1) break;
	}while((get_cur_off_time_tick() <= OFF_TIME_TICK_100MS_COUNT) && KEY_POWER);
	
	if((retry_count < 3) && (SendErrorOKFlag != 1) && KEY_POWER)
	{
		retry_count++;
		goto RETRY;
	}
}

//发送尿酸报错信息
void SendUAError(uint8_t errorcode)
{
	//uint8_t retry_count = 0;
	uint8_t user;
    if (g_s_set.e_user == USER_1)
        user= 0x01;
    else if (g_s_set.e_user == USER_2)
        user = 0x02;

	SendErrorOKFlag = 0;
RETRY:			
	framesize = 3;										//帧长
	SendHead();
	
	Rs232_SendByte(0xB5);							//数据类型--0xB5--发送尿酸报错
	Rs232_SendByte(user);
	Rs232_SendByte(errorcode);
	
	SendEnd();
	
	off_time_tick_clr();
	do
	{
		if(SendErrorOKFlag == 1) break;
	}while((get_cur_off_time_tick() <= OFF_TIME_TICK_100MS_COUNT) && KEY_POWER);
	
	if((retry_count < 3) && (SendErrorOKFlag != 1) && KEY_POWER)
	{
		retry_count++;
		goto RETRY;
	}
}
```


##### Bank 区分
Bank 0 最大，最常用的放在这里面，依次是 Bank 1、Bank 2、Bank 3

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_19.png]]

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_20.png]]
不能这么改，最好用默认值。Eprom 的 size 默认为 0x8000

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_21.png]]

报错如下，实际报错为 Bank 区超大小了：
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_22.png]]

修改前：

![[../../annex/20.入职第二十周11.24-11.29（单休）_image_23.png]]

修改后，将 queue.C 文件放在 Bank 1 中：
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_24.png]]

KE_2003项目，内存报错，bank 超了。bank0~3大小不一样，最常用的放在 bank0，依次往下，最不常用的放在 bank3。

KE_2003串口接收中断
```
void ble_decode_pro(void)
{
    uart_time_out_cnt++;
    if (uart_time_out_cnt < 3)
    {
        return;
    }
    uart_time_out_cnt = 0;
    ReceiveArray_index = 0;

    if (flag_is_uart_get_data == 1)
    {
        flag_is_uart_get_data = 0;
    }
    else
    {
        return;
    }
	if (ReceiveArray[0] == 0xFD && ReceiveArray[1] == 0xFD && ReceiveArray[2] == 0xFA)	//血压
	{
		switch (ReceiveArray[3])
		{
		case 0x04:
			ConnectFlag = 1;
			if (ReceiveArray[4] > 16 && ReceiveArray[4] < 100 && ReceiveArray[5] < 13 && ReceiveArray[6] < 32 && ReceiveArray[7] < 25) // Year Month Day Hour Minute Second
			{
			    g_s_time.year  = ReceiveArray[4]; // update date and time
			    g_s_time.month = ReceiveArray[5];
			    g_s_time.day = ReceiveArray[6];
			    g_s_time.hour = ReceiveArray[7];
			    g_s_time.min = ReceiveArray[8];
			    // Second = ReceiveArray[9];
			}
			break;
		case 0x05:
			ConnectFlag = 1;
			BTFlag = 1;
			IsPowerOn = 1;
			break;
		case 0x06:
			if ((ReceiveArray[4] == 0x0D) && (ReceiveArray[5] == 0x0A))
			{
				IsPowerOn = 0;	
			}
			break;
		case 0x07:
			if (ReceiveArray[4] > 16 && ReceiveArray[4] < 100 && ReceiveArray[5] < 13 && ReceiveArray[6] < 32 && ReceiveArray[7] < 25)  	//Year Month Day Hour Minute Second
			{
			    g_s_time.year  = ReceiveArray[4]; // update date and time
			    g_s_time.month = ReceiveArray[5];
			    g_s_time.day = ReceiveArray[6];
			    g_s_time.hour = ReceiveArray[7];
			    g_s_time.min = ReceiveArray[8];
			    // Second = ReceiveArray[9];
			}		
			SendMemoryFlag = 1;	
			break;
		case 0x08:
			if ((ReceiveArray[4] == 0x0D) && (ReceiveArray[5] == 0x0A))	//CLR ALL Memory
			{
			    ClrAllMemFlag = 1;
			}	
			break;
		case 0x09:
			if (ReceiveArray[5] == 0x0D && ReceiveArray[6] == 0x0A) // 更新用户
			{
			    if (ReceiveArray[4] == 1)
			        g_s_set.e_user = USER_1;
			    else if (ReceiveArray[4] == 2)
			        g_s_set.e_user = USER_2;
//			    else if (ReceiveArray[4] == 3)
//			        g_s_set.e_user = USER_3;
			    UserChangeF = 1;
			}
			break;
		case 0x0a:
			if ((ReceiveArray[4] == 0x0D) && (ReceiveArray[5] == 0x0A)) // RESET
			{
			    FacResetFlag = 1;
			}	
			break;
		case 0x60:
			SendOKFlag = 1;			
			break;	
		default:
			break;
		}		
	}
	else if((ReceiveArray[0] == 0xA5) && (ReceiveArray[1] == 0xA6) || (ReceiveArray[0] == 0x01))
	{
		ConnectFlag = 1;
			// BTFlag = 1;
			// IsPowerOn = 1;
	}
	
	if((ReceiveArray[0] == 0xA5) && (ReceiveArray[1] == 0xA5))	//血糖尿酸
	{
		switch (ReceiveArray[3])
		{
		case 0xC1:
			/*ConnectFlag = 1;
			SendStarttestFlag = 1;		//开始测量回复
			if (ReceiveArray[4] > 16 && ReceiveArray[4] < 100 && ReceiveArray[5] < 13 && ReceiveArray[6] < 32 && ReceiveArray[7] < 25) // Year Month Day Hour Minute Second
			{
			    g_s_time.year  = ReceiveArray[4]; // update date and time
			    g_s_time.month = ReceiveArray[5];
			    g_s_time.day = ReceiveArray[6];
			    g_s_time.hour = ReceiveArray[7];
			    g_s_time.min = ReceiveArray[8];
			    // Second = ReceiveArray[9];
			}
			break;*/
		case 0xC2:
			SendStarttestFlag = 1;
			break;
		case 0xC3:
			if ((ReceiveArray[4] == 0x5A) && (ReceiveArray[5] == 0x5A)) // 血糖结果数据回复
			{
				SendGLUOKFlag = 1;
			}
			break;
		case 0xC4:
			if ((ReceiveArray[4] == 0x5A) && (ReceiveArray[5] == 0x5A)) // 尿酸结果数据回复
			{
				SendUAOKFlag = 1;
			}
			break;
		case 0xC5:
			if ((ReceiveArray[4] == 0x5A) && (ReceiveArray[5] == 0x5A)) // 报错结果数据回复
			{
				SendErrorOKFlag = 1;
			}
			break;		
		case 0xC6:
			if ((ReceiveArray[4] == 0x5A) && (ReceiveArray[5] == 0x5A)) //  关机指令
			{
				shutdownFlag = 1;
			}
			break;	
		}
	}
	memset(ReceiveArray,0,sizeof(ReceiveArray));
}
```


## 20251014- U80RH
![[../../annex/20.入职第二十周11.24-11.29（单休）_image_25.png]]
CAM 是循环平均测量


```
A06添加工程模式、修改自动工装配置(有个引脚配置有问题)
```

```
cyl_test:
	SystolicValue1b = 120;
	DiastolicValue = 85;
	//SystolicValuew = 120;
	//DiastolicValuew = 85;
	PulseRate = 60;
```

## AXD-840 E 锂电

140mA→220mA

3.6 V→3.54 V 亮屏→3.34 V，3.16 V。
3.44 V
3.45 V 亮屏+语音播报
阀: 高电平关闭
马达: 不工作时 2 个引脚都是高电平，工作时一个引脚被拉低为低电平。


```
马林的机器，寿命模式第一次加压正常，第二次加压直接关机、无法唤醒(他测了此时电压为1.多V)，重新插入锂电池也没用，需要插USB激活一下才能唤醒。
原因:他把低电检测屏蔽了(预适应训练仪泵和阀比较多，耗电高，用仿真器进debug会仿真不了，所以屏蔽了低电检测。)，导致实际锂电池3.1V进入测量，换了个锂电池就正常了。
```


5.27V; 4.53V; 3.7V; 3.4V。

全显。LCD 拉低0.2-0.3V；语音播报拉低0.2V-0.3V；泵、阀拉低0.3-0.5V


```
void showpressure(void)
{
	if (T125ms == 0) OneShotDC = 0;
	else
	{
		if (OneShotDC == 0)
	     	{
	      		OneShotDC = 1;
	      		showpres();
                if (check_low_bat_trig(LOW_BAT_VAL - 35) == 1)
                {
                    g_s_flag_group.flag_low_bat_trig = 1;
                }
	     	}
	}
}
```

```
if (check_low_bat_trig(LOW_BAT_VAL - 35) == 1)
                {
                    g_s_flag_group.flag_low_bat_trig = 1;
                }
```

```
LOW_BAT_VAL=340V；340-35====305V
```

```
#if (AKY_STOP_MODE_DEBUG_ENABLE == 1)	
    if (!KEY_MEM)
    {
        while (!KEY_MEM)
        {
            /* code */
        }
        SystolicValuew = 222;
        DiastolicValue = 188;
        PulseRate = 80;
		MoveFlag = 1;
		PADFlag = 1;
		AFFlag = 1;
		flag_cuff_ok = 1;
        ResultCode = 0;
        IsAlgoEnd = 1;
        BB = POWER | VALVE | PUMP;
		iocontroloff(); 
     
    }
	if(!KEY_SET)
	{
		while (!KEY_SET)
        {
            /* code */
        }
		MoveFlag = 1;
		AFFlag=1;
	}
#endif
```


```
rail to rail 输入的比较器
```



## 培训--记忆存储
```
MemoryEndIndex存储的最新的物理地址
MemoryIndex总索引值（存了多少笔记忆）
MemoryFull记忆满（是否存满过）
```





内置可编程 FPG 时钟频率抑制功能



# 工作之余

#### 锁相环

#### 电阻的丝印下方的横杠

#### 电子负载仪的放电设置

#### PCB 和 PCBA

#### SOP

#### 8550 和 8050 三极管

#### 裸片 IC 和偏带 IC

#### 二极管有竖线的一段是负极（阴极）

#### C 语言中，声明和定义的区别、都会占内存吗？

#### 比较器的误差精度范围是多少？










































































































































































































